import { IDPConsentScreenManifest } from '@stytch/core/public';

type IDPConsentManifestGenerator = (input: {
    scopes: string[];
    clientName: string;
}) => IDPConsentScreenManifest;
type AuthTokenParams = {
    trustedAuthToken: string;
    tokenProfileID: string;
};
type B2BIDPConsentManifestGenerator = (input: {
    scopes: string[];
    clientName: string;
}) => IDPConsentScreenManifest;

export type { AuthTokenParams as A, B2BIDPConsentManifestGenerator as B, IDPConsentManifestGenerator as I };
